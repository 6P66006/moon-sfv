name = "6P66006/moon-sfv"

version = "0.1.0-dev"

readme = "README.md"

license = "Apache-2.0"

description = "RFC 9651 Structured Field Values parser, serializer, and conformance toolkit for MoonBit."

preferred_target = "native"

options(
  exclude: [ "_build/**" ],
)
